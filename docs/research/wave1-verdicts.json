[
  {
    "dim": "codec",
    "claim": "An AC-3 syncframe always contains 6 audio blocks x 256 samples = 1536 PCM samples per channel; this fixes encoder frame size and real-time granularity (32 ms at 48 kHz).",
    "verdict": "confirmed",
    "note": "Verified directly from ATSC A/52:2018 text (extracted from the official PDF): 'The spectral envelope and the coarsely quantized mantissas for 6 audio blocks (1536 audio samples per channel) are formatted into an AC-3 syncframe.' Each block carries 256 frequency coefficients. 1536/48000 = 32 ms is st"
  },
  {
    "dim": "codec",
    "claim": "AC-3 supports exactly three sample rates - 48, 44.1, and 32 kHz (fscod, A/52 Table 5.6); any other input must be resampled before encoding.",
    "verdict": "confirmed",
    "note": "A/52:2018 Section 5.4.1.3 / Table 5.6 (extracted from the official PDF): fscod '00'=48 kHz, '01'=44.1 kHz, '10'=32 kHz, '11'=reserved (decoder should mute). Independently corroborated by FFmpeg's ff_ac3_sample_rate_tab[] = { 48000, 44100, 32000, 0 }. The resampling consequence follows necessarily."
  },
  {
    "dim": "codec",
    "claim": "Legal AC-3 bit rates are 19 discrete CBR values from 32 to 640 kbit/s (32,40,48,56,64,80,96,112,128,160,192,224,256,320,384,448,512,576,640).",
    "verdict": "confirmed",
    "note": "A/52:2018 Table 5.18 (extracted from the official PDF) lists frmsizecod values (0) through (18) with nominal bit rates 32, 40, 48, 56, 64, 80, 96, 112, 128, 160, 192, 224, 256, 320, 384, 448, 512, 576, 640 kbps - exactly 19 discrete rates. FFmpeg's ff_ac3_bitrate_tab[19] contains the identical list."
  },
  {
    "dim": "codec",
    "claim": "AC-3 channel configurations are the 8 acmod modes (max 5 full-bandwidth channels) plus an optional LFE channel - maximum 5.1; bitstream channel order is L,C,R,SL,SR(+LFE last), which differs from WAV/",
    "verdict": "confirmed",
    "note": "A/52:2018 Table 5.8 (extracted from the official PDF) defines 8 acmod values ('000'-'111') with nfchans max 5 (3/2 mode: 'L, C, R, SL, SR'), plus the 1-bit lfeon flag - so 5.1 max. The audblk syntax confirms LFE is last: chmant[ch] loops over the full-bandwidth channels, then 'if (lfeon) ... lfemant"
  },
  {
    "dim": "codec",
    "claim": "FFmpeg's libavcodec AC-3 encoder accepts audio in FFmpeg-native (WAV-style) channel order and performs the AC-3 bitstream reorder internally, so the app never hand-reorders channels when using libavco",
    "verdict": "confirmed",
    "note": "Verified in current FFmpeg master ac3enc.c: it defines ac3_enc_channel_map[8][2][6] with the comment 'Table to remap channels from SMPTE order to AC-3 order. [channel_mode][lfe][ch]', and set_channel_info() assigns s->channel_map = ac3_enc_channel_map[s->channel_mode][s->lfe_on]. FFmpeg's native 5.1"
  },
  {
    "dim": "codec",
    "claim": "dialnorm is a mandatory 5-bit field (1-31 meaning -1 to -31 dBFS average dialogue level) that decoders use to shift playback level; FFmpeg's default of -31 applies no attenuation, so the app should me",
    "verdict": "confirmed",
    "note": "A/52:2018 Section 5.4.2.8 (extracted from the official PDF): dialnorm is a 5-bit unconditional bsi field; 'Valid values are 1-31. The value of 0 is reserved... interpreted as -1 dB to -31 dB with respect to digital 100 percent... The value of dialnorm shall affect the sound reproduction level.' FFmp"
  },
  {
    "dim": "codec",
    "claim": "All patents on AC-3 expired by 20 March 2017, so implementing and distributing an AC-3 encoder is royalty-free.",
    "verdict": "confirmed",
    "note": "Multiple independent sources agree: Wikipedia states 'The last patent covering AC-3 expired March 20, 2017, rendering it free to use'; contemporary coverage (EFF-sourced reporting, Hacker News, AVS Forum, Android Police - MX Player re-enabled AC-3 specifically because of the expiry) identifies the l"
  },
  {
    "dim": "codec",
    "claim": "\"Dolby\" and \"Dolby Digital\" remain Dolby Laboratories trademarks requiring a license for use in product names/logos, so the app (currently named 'dolbydigitalconverter') should brand its output as \"AC",
    "verdict": "confirmed",
    "note": "Dolby's own licensing materials confirm: most third-party use of Dolby trademarks and logos requires a licensing agreement (Logo Use Agreement / Trademark and Standardization Agreement for encoded content); purely referential use of the word marks (not logos) is permitted only if 'truthful, fair, an"
  },
  {
    "dim": "codec",
    "claim": "The vcpkg 'ffmpeg' port's default features (avcodec, avformat, swresample, ...) include the native LGPL AC-3 encoder with no extra feature flag or external codec dependency.",
    "verdict": "confirmed",
    "note": "vcpkg's ffmpeg vcpkg.json default-features are avcodec, avdevice, avfilter, avformat, swresample, swscale; no ac3/a52/dolby feature exists. The portfile.cmake passes only library-level flags (--enable-avcodec etc.) with no --disable-encoders/--disable-decoders/--disable-everything or codec-specific "
  },
  {
    "dim": "legal",
    "claim": "The last US patents covering core AC-3 (Dolby Digital) — US6449368 (expired 2017-03-14) and US5890106 (expired 2017-03-19/20) — lapsed in March 2017, making AC-3 encoding and decoding patent-free; Dol",
    "verdict": "confirmed",
    "note": "Google Patents independently confirms both patents: US6449368B1 'Multidirectional audio decoding' (Dolby Laboratories, filed 1997-03-14) status 'Expired - Lifetime', anticipated expiration 2017-03-14; and US5890106A (Dolby TDAC filter-bank patent, filed 1997-03-19) status 'Expired - Lifetime', antic"
  },
  {
    "dim": "legal",
    "claim": "Non-US AC-3 patents did not outlive the US ones (20-years-from-filing terms), and patent-conservative distributors such as Fedora have shipped AC-3 support in their fully-free FFmpeg builds since 2017",
    "verdict": "uncertain",
    "note": "Partly supported, partly wrong on specifics. The non-US part rests on community analysis (an AVS Forum review of Dolby's ETSI-declared essential patents finding the last one expired 2017-03-19; the thread is now behind a paywall/402 so I could only see it second-hand via search excerpts) — plausible"
  },
  {
    "dim": "legal",
    "claim": "The last E-AC-3 (Dolby Digital Plus) patent, US7516064B2 (Dolby Laboratories, 'Adaptive hybrid transform'), expired 2026-01-30 (adjusted term) and its status on Google Patents is 'Expired - Lifetime';",
    "verdict": "confirmed",
    "note": "Google Patents confirms US7516064B2 'Adaptive hybrid transform for signal analysis and synthesis', assignee Dolby Laboratories Licensing Corp, status 'Expired - Lifetime', adjusted expiration 2026-01-30. Phoronix published 'The Last Of The Dolby Digital Plus E-AC3 Patents Might Now Be Expired' on 20"
  },
  {
    "dim": "legal",
    "claim": "'Dolby', the double-D symbol, 'Dolby Digital', 'Dolby Digital Plus', 'Dolby Atmos' etc. are active Dolby trademarks; branding a product with them or using any Dolby logo requires a signed license/trad",
    "verdict": "confirmed",
    "note": "Dolby's own AC-4 delivery-kit trademark page states 'Dolby and the double-D symbol are registered trademarks of Dolby Laboratories' and lists Dolby Digital Plus, Dolby Atmos, Dolby Vision, etc. as marks (17 terms). Dolby's Terms of Use require express prior written consent for use of its marks/conte"
  },
  {
    "dim": "legal",
    "claim": "FFmpeg's built-in AC-3 encoders ('ac3' floating-point and 'ac3_fixed') are native implementations of ATSC A/52:2010 / ETSI TS 102 366 requiring no external library, and libavcodec/ac3enc.c is licensed",
    "verdict": "confirmed",
    "note": "FFmpeg's official codecs documentation states the ac3/ac3_fixed encoders 'implement part of ATSC A/52:2010 and ETSI TS 102 366', ac3 using floating-point and ac3_fixed fixed-point math, with no external library required. The current libavcodec/ac3enc.c header (fetched from FFmpeg master) carries the"
  },
  {
    "dim": "legal",
    "claim": "The vcpkg 'ffmpeg' port (v8.1.2 as of Aug 2026) defaults to features avcodec/avdevice/avfilter/avformat/swresample/swscale with 'gpl' and 'nonfree' as opt-in features, and the default Windows triplet ",
    "verdict": "confirmed",
    "note": "Fetched ports/ffmpeg/vcpkg.json from vcpkg master: version is '8.1.2'; default-features are exactly avcodec, avdevice, avfilter, avformat, swresample, swscale; 'gpl' ('the resulting libs and binaries will be under GPL') and 'nonfree' ('unredistributable') are optional, non-default features. Microsof"
  },
  {
    "dim": "legal",
    "claim": "Distributing a Windows app that dynamically links LGPL FFmpeg requires: prominent notice + LGPL license text + FFmpeg attribution, and making the corresponding FFmpeg source (with any modifications) a",
    "verdict": "confirmed",
    "note": "ffmpeg.org/legal.html's compliance checklist confirms the dynamic-linking case: compile without GPL/nonfree, use dynamic linking, mention FFmpeg/LGPLv2.1 in the about box and EULA, publish 'This software uses code of FFmpeg licensed under the LGPLv2.1 and its source can be downloaded here' with down"
  },
  {
    "dim": "encoders",
    "claim": "The current vcpkg ffmpeg port ships FFmpeg 8.1.2 (port-version 3), with avcodec, avformat, swresample, swscale, avfilter and avdevice as default features, consumed from CMake via find_package(FFMPEG) ",
    "verdict": "confirmed",
    "note": "Verified directly against the live port files on vcpkg master: vcpkg.json shows \"version\": \"8.1.2\", \"port-version\": 3, and default-features exactly [avcodec, avdevice, avfilter, avformat, swresample, swscale]. The port's usage file prescribes find_package(FFMPEG REQUIRED) with target_include_directo"
  },
  {
    "dim": "encoders",
    "claim": "FFmpeg 8.x removed all APIs deprecated before 6.0, including the old uint64_t channel_layout bitmask; new code must use the AVChannelLayout (ch_layout) API exclusively.",
    "verdict": "refuted",
    "note": "The version attribution is wrong: it was FFmpeg 7.0 \"Dijkstra\" (released 2024-04-05), not 8.x, that removed APIs deprecated before 6.0 — its announcement states the biggest change for library callers was removal of the old bitmask-based channel layout API in favor of AVChannelLayout. The claim's own"
  },
  {
    "dim": "encoders",
    "claim": "FFmpeg's native float \"ac3\" encoder accepts only AV_SAMPLE_FMT_FLTP (planar float) input and has a fixed frame_size of 1536 samples (6 blocks x 256); it declares AV_CODEC_CAP_DELAY (must be flushed wi",
    "verdict": "confirmed",
    "note": "Verified against FFmpeg master sources. ac3enc_float.c declares CODEC_SAMPLEFMTS(AV_SAMPLE_FMT_FLTP) — planar float only — and capabilities AV_CODEC_CAP_DR1 | AV_CODEC_CAP_DELAY | AV_CODEC_CAP_SMALL_LAST_FRAME | AV_CODEC_CAP_ENCODER_REORDERED_OPAQUE. ac3enc.c sets avctx->frame_size = AC3_BLOCK_SIZE "
  },
  {
    "dim": "encoders",
    "claim": "AC-3 supports only 32000/44100/48000 Hz sample rates and exactly 19 bit rates from 32 to 640 kbps (FFmpeg defaults: 96k mono, 192k stereo, 448k for 5 channels), with channel layouts from mono up to 5.",
    "verdict": "confirmed",
    "note": "Verified against FFmpeg master. ac3tab.c defines ff_ac3_sample_rate_tab as {48000, 44100, 32000, 0} and ff_ac3_bitrate_tab[19] = {32, 40, 48, 56, 64, 80, 96, 112, 128, 160, 192, 224, 256, 320, 384, 448, 512, 576, 640} — exactly 19 entries, 32–640 kbps. ac3enc.c's validate_options() sets defaults by "
  },
  {
    "dim": "encoders",
    "claim": "The modern encode loop is avcodec_send_frame/avcodec_receive_packet with EAGAIN/EOF handling, AVFrame set up with nb_samples = ctx->frame_size, format, ch_layout and av_frame_get_buffer, and end-of-st",
    "verdict": "confirmed",
    "note": "Verified against doc/examples/encode_audio.c on FFmpeg master: the encode() helper calls avcodec_send_frame(ctx, frame) then loops avcodec_receive_packet(ctx, pkt), returning on AVERROR(EAGAIN) || AVERROR_EOF; the frame is set up with frame->nb_samples = c->frame_size, frame->format = c->sample_fmt,"
  },
  {
    "dim": "encoders",
    "claim": "The last patent on core AC-3 (US 5,890,106) expired on 2017-03-20, making AC-3 encoding patent- and royalty-free; the expiry does NOT cover E-AC-3 (Dolby Digital Plus) or TrueHD/MLP, and \"Dolby Digita",
    "verdict": "confirmed",
    "note": "Substantially confirmed. Google Patents shows US 5,890,106 (Dolby Laboratories, TDAC filter bank) filed 1997-03-19, status 'Expired - Lifetime', anticipated expiration 2017-03-19 — contemporaneous coverage (GIGAZINE, HN 'expires at midnight' thread dated 2017-03-20, AVS Forum) and Wikipedia all trea"
  },
  {
    "dim": "input",
    "claim": "AC-3 (ATSC A/52) supports only 32, 44.1, and 48 kHz sample rates, so any capture at other rates (e.g. 96/192 kHz mixer settings) must be resampled before encoding; 48 kHz is the practical target.",
    "verdict": "confirmed",
    "note": "Independent verification via Wikipedia's Dolby Digital article confirms AC-3 sampling frequencies are exactly 32, 44.1, and 48 kHz (matching the ATSC A/52 fscod table). The hydrogenaudio wiki returned HTTP 403 and the ATSC PDF was not directly fetched, but the fact is uncontroversial and consistentl"
  },
  {
    "dim": "input",
    "claim": "FFmpeg's native 'ac3' encoder accepts only planar float input (AV_SAMPLE_FMT_FLTP), declared via CODEC_SAMPLEFMTS(AV_SAMPLE_FMT_FLTP) in libavcodec/ac3enc_float.c, so a float32 acquisition pipeline fe",
    "verdict": "confirmed",
    "note": "Fetched current master of libavcodec/ac3enc_float.c directly; it contains exactly 'CODEC_SAMPLEFMTS(AV_SAMPLE_FMT_FLTP),' as the sole declared sample format for the float AC-3 encoder. (Note: an integer fixed-point variant 'ac3_fixed' also exists taking S16P, but the default 'ac3' encoder is FLTP-on"
  },
  {
    "dim": "input",
    "claim": "The vcpkg 'ffmpeg' port is at version 8.1.2 and builds avcodec, avformat, and swresample as default features, so one vcpkg dependency covers file/network demux+decode, resampling/conditioning, and AC-",
    "verdict": "confirmed",
    "note": "Fetched the live ports/ffmpeg/vcpkg.json from vcpkg master: version is \"8.1.2\" and default-features are [\"avcodec\", \"avdevice\", \"avfilter\", \"avformat\", \"swresample\", \"swscale\"] — a superset of the three claimed. FFmpeg's native LGPL AC-3 encoder ships inside avcodec, so the conclusion holds. Minor c"
  },
  {
    "dim": "input",
    "claim": "In shared mode the Windows audio engine processes audio at an internal mix format (typically 32-bit float at the device mixer sample rate); IAudioClient::GetMixFormat returns this format and shared-mo",
    "verdict": "confirmed",
    "note": "Microsoft Learn's GetMixFormat page states verbatim that the method 'retrieves the stream format that the audio engine uses for its internal processing of shared-mode streams' and that 'the Initialize method always accepts the stream format obtained from a GetMixFormat call on the same device.' The "
  },
  {
    "dim": "input",
    "claim": "WASAPI loopback capture (AUDCLNT_STREAMFLAGS_LOOPBACK on a render endpoint) works only for shared-mode streams, and event-driven loopback is natively supported on Windows 10 1703+ (no render-stream ev",
    "verdict": "confirmed",
    "note": "Microsoft's Loopback Recording page confirms both parts verbatim: 'A client can enable loopback mode only for a shared-mode stream (AUDCLNT_SHAREMODE_SHARED). Exclusive-mode streams cannot operate in loopback mode,' and 'In Windows 10 versions 1703 and higher, event-driven loopback clients are suppo"
  },
  {
    "dim": "input",
    "claim": "WASAPI loopback delivers no capture packets while no audio is being rendered, so a real-time AC-3 encoder pipeline must synthesize silence to maintain a continuous 48 kHz output clock.",
    "verdict": "confirmed",
    "note": "Confirmed as the prevailing (though driver-dependent) behavior. PortAudio issue #935 documents callbacks never firing and reads blocking when nothing is playing on multiple Windows 7/10 systems; Matthew van Eerde's canonical loopback-capture sample and Microsoft Q&A threads corroborate that no packe"
  },
  {
    "dim": "input",
    "claim": "miniaudio supports Windows loopback capture out of the box via ma_device_type_loopback (WASAPI backend only), is actively maintained (v0.11.25 released March 2026), and the vcpkg 'miniaudio' port ship",
    "verdict": "confirmed",
    "note": "All three parts verified independently: the miniaudio manual states it 'supports playback, capture, full-duplex and loopback (WASAPI only)' via ma_device_type_loopback; GitHub releases show v0.11.25 released March 3, 2026 (with v0.11.24 Jan 2026 and v0.11.23 Sep 2025 showing active maintenance); and"
  },
  {
    "dim": "input",
    "claim": "The vcpkg 'portaudio' port pins the v19.7.0 (April 2021) commit 147dd722548358763a8b649b3e4b41dfffbcfbb6, which predates the WASAPI loopback support present in PortAudio master (loopback was still an ",
    "verdict": "confirmed",
    "note": "Every element checks out: the live vcpkg portfile.cmake pins REF 147dd722548358763a8b649b3e4b41dfffbcfbb6 (vcpkg.json: version \"19.7\", port-version 9); the GitHub git-refs API confirms the v19.7.0 tag points to exactly that commit, dated April 1, 2021; issue #668 (Feature Request: WASAPI Loopback Re"
  },
  {
    "dim": "input",
    "claim": "libswresample performs sample-rate conversion, sample-format conversion (incl. interleaved float32 to planar fltp), and channel rematrixing in a single SwrContext, making it a one-stop conditioning st",
    "verdict": "confirmed",
    "note": "Fetched libswresample/swresample.h from FFmpeg master: it describes itself as an 'Audio resampling, sample format conversion and mixing library' and its own doc example performs rate conversion (48k->44.1k), planar/packed format conversion, and channel-layout rematrixing (5.1->stereo downmix with de"
  },
  {
    "dim": "output",
    "claim": "An AC-3 elementary stream is a sequence of self-contained sync frames, each beginning with 16-bit syncword 0x0B77 and containing 6 audio blocks x 256 = 1536 samples per channel; frame byte-size is det",
    "verdict": "confirmed",
    "note": "RFC 4184 states each AC-3 frame starts with syncword 0x0B77 and contains 6 audio blocks of 256 PCM samples (1536 samples total), with frame size determined by sample rate and data rate via A/52 Table 5.18 (frame size code table, 128-3840 bytes). Wikipedia confirms syncword 0x0B77 and the fscod/frmsi"
  },
  {
    "dim": "output",
    "claim": "FFmpeg's native ac3 encoder supports sample rates 48000/44100/32000 Hz, planar-float input (fltp), channel layouts up to 5.1, bitrates up to 640 kbit/s, and full Dolby metadata options (dialnorm, dsur",
    "verdict": "confirmed",
    "note": "Independently re-ran 'ffmpeg -h encoder=ac3' on the locally installed FFmpeg 8.0.1 (Gyan build): supported sample rates are exactly 48000/44100/32000, sole sample format is fltp, channel layouts run mono through 5.1(side)/5.1, and the AVOptions list includes dialnorm, dsur_mode, center_mixlev, surro"
  },
  {
    "dim": "output",
    "claim": "IEC 61937 wraps each AC-3 frame in a data burst with preamble words Pa=0xF872, Pb=0x4E1F, Pc=burst-info (data-type 1 = AC-3 in bits 0-6, bsmod in bits 8-12), Pd=payload length in bits, zero-padded to ",
    "verdict": "confirmed",
    "note": "FFmpeg spdif.h defines SYNCWORD1 0xF872, SYNCWORD2 0x4E1F, IEC61937_AC3 = 0x01; spdifenc.c sets ctx->data_type = IEC61937_AC3 | (bitstream_mode << 8) (bsmod in bits 8+), ctx->pkt_offset = AC3_FRAME_SIZE << 2, and length_code in bits (size << 3); ac3defs.h defines AC3_FRAME_SIZE = 6 * 256 = 1536, so "
  },
  {
    "dim": "output",
    "claim": "Windows (since Windows 7) represents AC-3 passthrough with WAVEFORMATEXTENSIBLE_IEC61937 (cbSize=34, total 52 bytes) using SubFormat GUID KSDATAFORMAT_SUBTYPE_IEC61937_DOLBY_DIGITAL = 00000092-0000-00",
    "verdict": "confirmed",
    "note": "Microsoft Learn confirms every element verbatim: WAVEFORMATEXTENSIBLE_IEC61937 introduced in Windows 7 (KsMedia.h), cbSize must be 34 and total size 52 bytes; the SubFormat GUID table lists CEA-861 type 0x02 = 00000092-0000-0010-8000-00aa00389b71 = KSDATAFORMAT_SUBTYPE_IEC61937_DOLBY_DIGITAL, descri"
  },
  {
    "dim": "output",
    "claim": "WASAPI exclusive mode bypasses the Windows audio engine's mixing/resampling entirely and session-volume interfaces have no effect on exclusive streams — which is exactly the bit-exactness passthrough ",
    "verdict": "confirmed",
    "note": "Microsoft's exclusive-mode-streams page confirms: exclusive-mode streams give the app exclusive use of the endpoint device (shared-mode mixing is bypassed; the page even contrasts shared mode as 'preserving system sound mixing' and recommends exclusive only for 'a hard requirement for bit-exact outp"
  },
  {
    "dim": "output",
    "claim": "All relevant AC-3 (Dolby Digital) patents expired by 2017-03-20, making software AC-3 encoding patent-royalty-free; E-AC-3's last US patent (US7516064) expired 2026-01-30, but E-AC-3 caution is still ",
    "verdict": "confirmed",
    "note": "Multiple independent sources (EFF coverage, AVS Forum, Hacker News, Gigazine) document that Dolby's last AC-3-relevant patents expired by March 20, 2017 (the final ones being US6449368, expiring 2017-03-14, and US5890106, expiring 2017-03-19), after which AC-3 became royalty-free — Dolby itself stop"
  },
  {
    "dim": "output",
    "claim": "'Dolby', 'Dolby Digital' and the double-D logo remain registered trademarks requiring a Dolby agreement for product branding/logo use; the format may be referred to as 'AC-3' freely, and nominative re",
    "verdict": "confirmed",
    "note": "USPTO records (via Justia) show 'DD DOLBY DIGITAL' registered to Dolby Laboratories Licensing Corporation (e.g., registration 3998602), and countless product manuals (Sony, LG, Yamaha) carry the notice that Dolby and the double-D symbol are registered trademarks used under license. Dolby's professio"
  },
  {
    "dim": "output",
    "claim": "FFmpeg's float AC-3 encoder is vastly faster than real time on a modern desktop: measured locally at ~304x real time encoding 5.1 @ 640 kbit/s / 48 kHz (600 s of audio in ~1.97 s, ~39 MiB RSS, single-",
    "verdict": "confirmed",
    "note": "Independently re-benchmarked on this machine with FFmpeg 8.0.1: encoding 600 s of 5.1 48 kHz noise at 640 kbit/s reported speed=289x, utime=2.266 s, rtime=2.077 s, maxrss=42,244 KiB (~41 MiB) — closely matching the claimed ~304x / ~1.97 s / ~39 MiB (exact figures vary by CPU, same order). Per-frame "
  },
  {
    "dim": "output",
    "claim": "vcpkg's ffmpeg port (version 8.1.2, port-version 3) builds with MSVC for x64-windows and x64-windows-static with default features avcodec/avformat/swresample among others; the native AC-3 encoder and ",
    "verdict": "confirmed",
    "note": "vcpkg master's ports/ffmpeg/vcpkg.json shows exactly version 8.1.2, port-version 3, with default-features [avcodec, avdevice, avfilter, avformat, swresample, swscale] — so avcodec/avformat/swresample are defaults as claimed. x64-windows and x64-windows-static are standard vcpkg triplets for this por"
  },
  {
    "dim": "build",
    "claim": "There is no /std:c++23 switch in any shipping MSVC as of August 2026; C++23 is enabled via /std:c++23preview (available since VS 2022 17.13 Preview 4, ABI not guaranteed stable) or /std:c++latest; /st",
    "verdict": "confirmed",
    "note": "Microsoft's /std docs list only /std:c++14, c++17, c++20, c++23preview, c++latest (no /std:c++23), state c++23preview is 'Available starting in Visual Studio 2022 version 17.13 Preview 4' and that 'Preview features may change and may not be ABI compatible across releases', and that the switch will b"
  },
  {
    "dim": "build",
    "claim": "CMake (current master, applies to 3.x and 4.x releases) maps CMAKE_CXX_STANDARD 23 / cxx_std_23 to -std:c++latest for MSVC >= 19.29.30129 and never emits /std:c++23preview, so setting standard 23 on M",
    "verdict": "confirmed",
    "note": "Verified in Modules/Compiler/MSVC-CXX.cmake on both current master and the released v3.30.0 tag: inside 'if(CMAKE_CXX_COMPILER_VERSION VERSION_GREATER_EQUAL 19.29.30129)' it sets CMAKE_CXX23_STANDARD_COMPILE_OPTION (and EXTENSION option) to \"-std:c++latest\"; no occurrence of c++23preview anywhere in"
  },
  {
    "dim": "build",
    "claim": "The vcpkg registry's ffmpeg port is currently version 8.1.2 port-version 3, with default features avcodec, avdevice, avfilter, avformat, swresample, swscale - so avcodec, avformat, and swresample are ",
    "verdict": "confirmed",
    "note": "The live ports/ffmpeg/vcpkg.json on microsoft/vcpkg master shows \"version\": \"8.1.2\", \"port-version\": 3, and default-features exactly [avcodec, avdevice, avfilter, avformat, swresample, swscale]; avcodec, avformat, and swresample are all present."
  },
  {
    "dim": "build",
    "claim": "The vcpkg ffmpeg port is consumed in CMake via the port-shipped FindFFMPEG module: find_package(FFMPEG REQUIRED) providing the variables FFMPEG_INCLUDE_DIRS, FFMPEG_LIBRARY_DIRS, and FFMPEG_LIBRARIES ",
    "verdict": "confirmed",
    "note": "The port's usage file shows exactly find_package(FFMPEG REQUIRED) with FFMPEG_INCLUDE_DIRS, FFMPEG_LIBRARY_DIRS, and FFMPEG_LIBRARIES; it mentions no imported targets, and lists pkg-config modules libavcodec, libavdevice, libavfilter, libavformat, libavutil, libswresample, libswscale."
  },
  {
    "dim": "build",
    "claim": "The vcpkg ffmpeg portfile leaves FFmpeg's native codecs (including the built-in AC-3 encoder in libavcodec) enabled - it does not pass --disable-everything - and passes --enable-gpl only when the 'gpl",
    "verdict": "confirmed",
    "note": "portfile.cmake never passes --disable-everything, --disable-encoders, or --disable-decoders; --enable-gpl is emitted only under 'if(\"gpl\" IN_LIST FEATURES)' (likewise --enable-nonfree/--enable-version3 for their features); nothing disables built-in libavcodec codecs such as the ac3 encoder. The port"
  },
  {
    "dim": "build",
    "claim": "FFmpeg's official LGPL compliance guidance requires compiling without --enable-gpl/--enable-nonfree and recommends dynamic linking (DLLs on Windows); static linking is only compliant if you additional",
    "verdict": "refuted",
    "note": "Partially wrong as an attribution. Confirmed parts: the ffmpeg.org legal checklist does say compile 'without --enable-gpl and without --enable-nonfree', 'Use dynamic linking (on windows, this means linking to dlls)', and requires distributing the exactly-corresponding FFmpeg source (hosted on the sa"
  },
  {
    "dim": "build",
    "claim": "Version pinning in manifest mode is done by adding a builtin-baseline (40-char git SHA of microsoft/vcpkg) to vcpkg.json, most easily via 'vcpkg x-update-baseline --add-initial-baseline'; a vcpkg-conf",
    "verdict": "confirmed",
    "note": "Microsoft's docs confirm: x-update-baseline --add-initial-baseline (manifest mode only) will 'Add a \"builtin-baseline\" field to the vcpkg.json if it does not already have one'; the vcpkg-configuration.json reference states baselines for git/builtin registries are 'a 40-character git commit sha' and "
  },
  {
    "dim": "build",
    "claim": "Official vcpkg guidance wires the toolchain through CMakePresets.json with CMAKE_TOOLCHAIN_FILE=$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake in cacheVariables; all vcpkg-affecting variables (VCPK",
    "verdict": "confirmed",
    "note": "The vcpkg CMake-integration page (updated 2026-07) confirms every part: 'We recommend using CMake Presets to specify your toolchain file' with the exact cacheVariables example CMAKE_TOOLCHAIN_FILE=$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake; 'All vcpkg-affecting variables must be defined befor"
  },
  {
    "dim": "build",
    "claim": "Exact vcpkg port names and current versions: miniaudio 0.11.25, libsndfile 1.2.2#2 (default features external-libs and mpeg), drlibs 2026-03-03 (the dr_wav/dr_flac/dr_mp3 collection is named 'drlibs',",
    "verdict": "confirmed",
    "note": "Verified against the live port manifests on microsoft/vcpkg master: miniaudio 0.11.25 (no port-version); libsndfile version-semver 1.2.2 port-version 2 with default-features [external-libs, mpeg]; drlibs name 'drlibs', version-date 2026-03-03; portaudio 19.7 port-version 9; sdl3 3.4.14 (no port-vers"
  },
  {
    "dim": "build",
    "claim": "Using the Ninja generator with MSVC outside the IDE requires the vcvars developer environment (cl.exe on PATH); Visual Studio and VS Code auto-load vcvars for Ninja presets; the Visual Studio generato",
    "verdict": "confirmed",
    "note": "MS docs (cmake-presets-vs, 'Sourcing the environment when building with command-line generators on Windows'): 'If you're building with Ninja and the Microsoft C++ Build Tools on Windows, set the environment before CMake is called... by calling vcvarsall.bat'; 'If you build on Linux or on Windows wit"
  }
]
